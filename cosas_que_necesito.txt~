Adjust the font (somehow) so that s <-> f in the correct places. 
There is no italic DayRomanS font. 
Need a header which matches the book. 
Need an environment for propositions which matches the book and adjusts the
toc. 
Figure out how to add figures in parallel with the text. 
Fancy lettering (beginning props) and fancy header. 

